#set text(font: "Iosevka NF")
#show heading: it => [#h(-5mm) #text(weight: "medium", fill: gray)[\#] #text(
    weight: "black",
    font: "JetBrains Mono",
    it.body,
  ) \ #v(-3mm)]

= JSON Web Tokens
#lorem(30)

```python
def add(a: int, b: int) -> int:
    return a + b
```
